{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "go",
            "preLaunchTask": "build",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}",
            "args": [
                "-p=9001",
                "-config=local"
            ]
        }
    ]
}