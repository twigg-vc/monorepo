# Pull
Pulls commits from the server to local.

Pull all submitted commits.
```
tw pull
```

Pull specific pending commit from server. Use the [server commit syntax](./commit-syntax.md#server-syntax).
```
tw pull <server commit>
```