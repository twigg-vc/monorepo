Just to keep things organized, this folder contains all data/storage/compression
helper packages. The only exceptions are concrete implementations of databases
used by specific services; such as the CLI db and the webserver db. 